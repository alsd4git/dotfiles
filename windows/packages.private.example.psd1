@{
    # Copy this file to ~/.config/dotfiles/windows/packages.private.psd1 for local-only extras.
    # Keep private packages out of the public repo and use this file as an untracked scaffold.

    Winget = @(
    )

    Scoop = @(
    )

    Chocolatey = @(
    )

    NpmGlobal = @(
    )
}
